We need to show that (∀ a ∈ ℝ,
                        ∀ b ∈ ℝ,
                        a < b
                        ⇨ a is a _lower bound_ for [a, b)
                          ∧ (∀ l ∈ ℝ, l is a _lower bound_ for [a, b) ⇨ l ≤ a)).